<UserControl x:Class="NullBot.UI.Views.QuarantineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NullBot.UI.ViewModels"
             xmlns:conv="clr-namespace:NullBot.UI.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:QuarantineViewModel}"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <conv:BytesToSizeConverter x:Key="ByteSize"/>
        <conv:RelativeTimeConverter x:Key="RelTime"/>
    </UserControl.Resources>

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Quarantine" Style="{StaticResource HeadlineStyle}" Margin="0,0,0,20"/>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource DarkTextBoxStyle}"
                     Margin="0,0,12,0"/>

            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding FilterTypes}"
                      SelectedItem="{Binding FilterType}"
                      Style="{StaticResource DarkComboBoxStyle}"
                      Margin="0,0,12,0"/>

            <Button Grid.Column="2"
                    Content="Delete &gt;30 days"
                    Style="{StaticResource DangerButtonStyle}"
                    Command="{Binding DeleteOldItemsCommand}"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredItems}"
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource DarkDataGridStyle}"
                  Background="{StaticResource PanelBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"            Binding="{Binding Id}"            Width="80"/>
                <DataGridTextColumn Header="Threat Name"   Binding="{Binding ThreatName}"    Width="*"/>
                <DataGridTextColumn Header="Original Path" Binding="{Binding OriginalPath}"  Width="2*"/>
                <DataGridTextColumn Header="Type"          Binding="{Binding DetectionType}" Width="90"/>
                <DataGridTextColumn Header="Date"          Binding="{Binding QuarantinedAt, Converter={StaticResource RelTime}}" Width="100"/>
                <DataGridTextColumn Header="Size"          Binding="{Binding SizeBytes, Converter={StaticResource ByteSize}}"    Width="80"/>
                <DataGridTemplateColumn Header="Actions" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="8,4">
                                <Button Content="Restore"
                                        Style="{StaticResource GhostButtonStyle}"
                                        FontSize="11" Padding="8,4"
                                        Command="{Binding DataContext.RestoreItemCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,6,0"/>
                                <Button Content="Delete"
                                        Style="{StaticResource DangerButtonStyle}"
                                        FontSize="11" Padding="8,4"
                                        Command="{Binding DataContext.DeleteItemCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer: export -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Export CSV"
                    Style="{StaticResource GhostButtonStyle}"
                    Command="{Binding ExportReportCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
